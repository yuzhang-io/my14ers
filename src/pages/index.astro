---
import BaseLayout from "../layouts/BaseLayout.astro";
import FourteenerMap from "../components/FourteenerMap.astro";
---

<BaseLayout title="my14ers">
  <div class="app-shell">
    <main class="mx-auto max-w-6xl px-6 py-10">
    <header class="flex items-end justify-between gap-4">
      <div>
        <h1 class="text-4xl font-semibold tracking-tight">
          <span class="bg-linear-to-r from-fuchsia-400 via-violet-300 to-cyan-300 bg-clip-text text-transparent">
            my14ers
          </span>
        </h1>
        <p class="mt-2 text-sm text-zinc-400">
          Mark climbs → generate a futuristic shareable snapshot.
        </p>
      </div>

      <div class="rounded-2xl border border-white/10 bg-black/20 px-4 py-2 text-xs text-zinc-400 backdrop-blur">
        Export a styled PNG directly from the checklist controls below.
      </div>
    </header>

    <div class="mt-6">
      <FourteenerMap />
    </div>

    <footer class="mt-10 text-xs text-zinc-500">
      Data stays in your browser (localStorage). Export a JSON to save/share.
    </footer>
  </main>
</div>
</BaseLayout>